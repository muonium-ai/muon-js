[package]
name = "muon-js"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "muon_js"
path = "src/lib.rs"

[features]
# Keep default minimal; add feature flags as ports land.
default = []

[dependencies]
regex = "1.10.3"

[dev-dependencies]

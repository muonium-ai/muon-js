---
id: T-000006
title: Replace builtin identifier if-chain with static dispatch map
status: needs_review
priority: p1
type: code
effort: m
labels:
- performance
tags:
- eval
owner: agent-1
created: '2026-02-20'
updated: '2026-02-20'
depends_on:
- T-000002
branch: bug/t-000006-replace-builtin-identifier-if-chain-with
---

## Goal
Replace long builtin-name if chains with table-driven dispatch to reduce repeated comparisons on eval hot path.

## Acceptance Criteria
- [ ] Replace linear builtin name checks in eval hot path with a table-driven/static dispatch strategy.
- [ ] Preserve current fallback behavior for global overrides and undefined builtins.
- [ ] Add benchmark coverage showing reduced lookup overhead for repeated builtin references.
- [ ] Keep all existing builtin behavior tests passing.

## Notes
- Candidate area: builtin resolution chain in `src/evals.rs` (`eval_value`).
- Test plan:
	- Add microbenchmark for repeated resolution of common builtins (`Math`, `Object`, `Array`, `JSON`).
	- Run full sample suite and core tests (`cargo test`, shell tests).
	- Compare behavior with upstream mquickjs for relevant identifier resolution cases.

## Progress Log
- 2026-02-20: Implemented static builtin dispatch map in src/evals.rs: replaced long if-chain with sorted const BUILTIN_DISPATCH table plus binary_search lookup, preserving builtin_or_global fallback behavior. Added builtin_identifier_lookup workload in src/bin/bench_runtime.rs and updated performance.md. Validation: cargo test --lib passed (64/64); make js-runtime-bench passed with output tmp/comparison/js_runtime_benchmark_20260220_152653.json (builtin_identifier_lookup median ~137525 ops/s).

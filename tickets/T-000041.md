---
id: T-000041
title: Fix all cargo build warnings
status: claimed
priority: p2
type: chore
effort: s
labels: [cleanup]
tags: [mini-redis, build]
owner: null
created: 2026-02-21
updated: 2026-02-21
depends_on: []
branch: null
score: 50.0
---

# T-000041: Fix all cargo build warnings

## Goal

Eliminate all compiler warnings emitted by `cargo check --features mini-redis` to keep
the build output clean. Currently 10 warnings in `src/mini_redis/persist.rs` (unused
imports, unused variables) and `src/mini_redis/server.rs` (unused variable).

## Warnings to fix

1. `persist.rs:5` — unused imports: `Receiver`, `Sender`, `bounded`
2. `persist.rs:6` — unused import: `async_std::future::timeout`
3. `persist.rs:7` — unused import: `async_std::task`
4. `persist.rs:8` — unused import: `std::time::Duration`
5. `persist.rs:37` — unused variable: `dbs` (in `load`)
6. `persist.rs:53` — unused variable: `dbs` (in `snapshot`)
7. `persist.rs:45` — unused variables: `db`, `cmd`, `args` (in `log_command`)
8. `server.rs:633` — unused variable: `script`

## Acceptance criteria

- [ ] AC1: `cargo check --features mini-redis` produces zero warnings.
- [ ] AC2: All 66 tests pass.

## Test plan

- `cargo check --features mini-redis 2>&1 | grep warning` — expect empty.
- `cargo test --features mini-redis` — all pass.

---
id: T-000060
title: Support labeled statements in JS parser
status: done
priority: p2
type: code
effort: m
labels: [bug, parser]
tags: [muonjs, api, parser]
owner: agent-1
created: 2026-02-21
updated: 2026-02-21
depends_on: []
branch: bug/t-000060-support-labeled-statements
---

## Goal
Implement labeled statement syntax (label: { break label; }) so test_language.js test_labels() and test_labels2() pass

## Current state

The mquickjs `test_language.js` file has two test functions that use labeled statements:

```javascript
// test_labels()
do x: { break x; } while(0);
if (1) x: { break x; } else x: { break x; }
while (0) x: { break x; };

// test_labels2()
while (1) label: break
for (;;) label: break
for (i = 0; i < 3; i++) label: { if (i > 0) break; }
```

These fail with `SyntaxError` because the parser doesn't recognize `identifier:` as a
label prefix. All other tests in test_language.js pass.

## Acceptance Criteria
- [x] `label: { break label; }` exits the labeled block
- [x] `label: break` works as a labeled break in loops
- [x] `do label: { break label; } while(0)` works
- [x] `bash tests/run_mquickjs_tests.sh` — test_language.js passes fully
- [x] All cargo tests pass, 10/10 integration tests pass

## Notes
Labeled statements are part of the ECMAScript spec. Both the tree-walking interpreter
(eval_expr/split_statements in api.rs/evals.rs) and the bytecode compiler (compiler.rs)
may need updates.

## Progress Log
- 2026-02-21: Reproduced label failures and isolated root cause to cached statement execution path in `eval_function_body_cached` bypassing label parsing for identifier-leading statements.
- 2026-02-21: Added labeled-statement handling in cached fast path before `eval_expr`, fixing `do x: { break x; } while(0)` and generic identifier labels in cached loop bodies.
- 2026-02-21: Added regressions `do_while_with_labeled_block_break` and `labeled_statement_in_cached_loop_body_any_identifier`.
- 2026-02-21: Validation: `cargo test --features mini-redis` (70 pass), `bash tests/run_integration.sh` (10/10), `bash tests/run_mquickjs_tests.sh` with `test_language.js` passing fully (suite still reports unrelated `test_builtin.js` failure).
6-02-21: Implemented cached-path labeled statement handling in eval_function_body_cached (identifier-leading labels now parsed before eval_expr), added two regressions for do-while labeled block and generic identifier label in cached loop body. Validation: cargo test --features mini-redis (70 pass), integration 10/10, mquickjs test_language.js PASS (suite still has unrelated test_builtin.js failure).
- 2026-02-21: Fixed test_builtin TypeError path: VM LoadGlobal builtin fallback + closure frame slot update; added targeted regression tests.
- 2026-02-21: Fixed test_builtin.js infinite hang at test_function: root cause was parse_lvalue returning None for non-integer bracket keys (e.g. `a[1.2]`), causing TypeError to be lost in try/catch. Three fixes: (1) parse_lvalue now handles float bracket keys via `ctx.float_value()` and falls back to `js_to_string` for other types, (2) assignment handlers return `Some(Value::EXCEPTION)` instead of `None` on exception, (3) parse_try_catch `None` branch now preserves exception value via `ctx.get_exception()`. test_builtin.js now runs through test_json before hitting a pre-existing `f()+g()` parser SyntaxError. Validation: cargo test --features mini-redis (73 pass), integration 10/10.

---
id: T-000029
title: Fast-path SELECT outside DB lock
status: done
priority: p0
type: code
effort: s
labels: [performance]
tags: [mini-redis, locking, command-path]
owner: agent-1
created: 2026-02-21
updated: 2026-02-21
depends_on: [T-000028]
branch: bug/t-000029-fast-path-select-outside-db-lock
---

## Goal
Reduce lock contention by handling SELECT without dbs_state lock in normal and EXEC queue paths.

## Acceptance Criteria
- [ ] Fast-path `SELECT` command execution in normal command flow before DB lock acquisition.
- [ ] Apply the same `SELECT` fast-path in `EXEC` queued command processing.
- [ ] Preserve `SELECT` semantics and invalid-index error behavior.
- [ ] `cargo check --bin mini_redis --features mini-redis` succeeds.
- [ ] `cargo test --lib` remains green.

## Notes

## Progress Log
- 2026-02-21: Created and claimed as next micro-step after T-000028.
- 2026-02-21: Updated `handle_no_db_command` to process `SELECT` using a precomputed DB count, updating `current_db` without taking the DB mutex.
- 2026-02-21: Routed both normal and EXEC paths through the enhanced no-DB handler for `SELECT`.
- 2026-02-21: Validation: `cargo check --bin mini_redis --features mini-redis` passed; `cargo test --lib` passed (64/64).
- 2026-02-21: Implemented SELECT fast-path in normal and EXEC flows via no-DB command handler; db index now updates without db lock using precomputed db count. Validation passed (cargo check --bin mini_redis --features mini-redis, cargo test --lib 64/64).

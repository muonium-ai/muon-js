---
id: T-000028
title: Fast-path SAVE/BGSAVE outside DB lock
status: done
priority: p0
type: code
effort: m
labels: [performance]
tags: [mini-redis, locking, persistence]
owner: agent-1
created: 2026-02-21
updated: 2026-02-21
depends_on: [T-000027]
branch: bug/t-000028-fast-path-save-bgsave-outside-db-lock
---

## Goal
Reduce lock contention by handling SAVE/BGSAVE in handle_client without holding dbs_state lock during persistence I/O.

## Acceptance Criteria
- [ ] Add dedicated SAVE/BGSAVE fast-path in `handle_client` that avoids wrapping persistence I/O with the long-lived `dbs_state` lock.
- [ ] Keep SAVE/BGSAVE command semantics and error responses unchanged.
- [ ] Apply the same fast-path in `EXEC` queue processing for queued SAVE/BGSAVE commands.
- [ ] `cargo check --bin mini_redis --features mini-redis` succeeds.
- [ ] `cargo test --lib` remains green.

## Notes

## Progress Log
- 2026-02-21: Created and claimed as next decomposition step after T-000027.
- 2026-02-21: Implemented `handle_save_like_command` and `snapshot_dbs_for_persistence` in `src/mini_redis/server.rs`; SAVE/BGSAVE now snapshot DBs under lock and run persistence on snapshot outside the DB mutex.
- 2026-02-21: Routed both normal command path and EXEC queued command path through SAVE/BGSAVE fast-path before DB lock acquisition.
- 2026-02-21: Validation: `cargo check --bin mini_redis --features mini-redis` passed; `cargo test --lib` passed (64/64).
- 2026-02-21: Implemented SAVE/BGSAVE fast-path: snapshot DBs under lock and persist snapshot outside DB lock in normal and EXEC queued flows. Validation passed (cargo check --bin mini_redis --features mini-redis, cargo test --lib 64/64).

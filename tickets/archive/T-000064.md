---
id: T-000064
title: "Perf: set_members 0.39x — Lua 2.6x faster (multi-SADD + SMEMBERS loop)"
status: needs_review
priority: p1
type: code
effort: m
labels: [performance, scripting]
tags: [muonjs, mini-redis]
owner: agent-1
created: 2026-02-22
updated: 2026-02-22
depends_on: []
branch: bug/t-000064-perf-set-members-0-39x-lua-2-6x-faster-m
score: 220.0
---

## Goal
Close the gap on the `set_members` scripting benchmark where Lua is 2.6x faster than muonjs.

## Current state

Benchmark ratio: **0.39x** (js_rps / lua_rps) — Lua is ~2.6x faster.

Script (`10_set_members.js`):
```js
redis.call('DEL', KEYS[0]);
var n = Number(ARGV[0] || 100);
for (var i = 1; i <= n; i += 1) {
  redis.call('SADD', KEYS[0], 'm' + i);
}
var members = redis.call('SMEMBERS', KEYS[0]);
return members.length;
```

This is a loop-heavy script: 100 iterations of `redis.call('SADD', ...)` with string
concatenation (`'m' + i`), plus a final SMEMBERS + `.length` access.

## Investigation areas
- **String concatenation cost**: `'m' + i` creates a new string each iteration — could be
  expensive if it goes through full coercion path
- **Loop overhead**: `for` loop with `var i = 1; i <= n; i += 1` — check if loop body
  evaluation, variable lookup, and increment have avoidable overhead
- **Number() coercion**: `Number(ARGV[0] || 100)` goes through full type coercion
- **SADD fast path**: Already exists but verify it's being hit for single-member calls
- **Property access `.length`**: `members.length` on an array — check overhead
- **Per-iteration redis.call dispatch**: Each call goes through command parsing even with
  fast path — 100 calls adds up

## Acceptance Criteria
- [ ] `set_members` benchmark ratio improves above 0.60x
- [ ] `make lua-js-perf-check` passes gate
- [ ] All cargo tests pass, 10/10 integration tests pass

## Test plan
- `cargo test`
- `make lua-js-perf-check` — check set_members ratio
- `bash tests/run_mquickjs_tests.sh`

## Progress Log
- 2026-02-22: Implementation complete. Results: set_members 0.39x → 1.80x (4.6x speedup), overall mean 2.00x → 2.60x. All 71 cargo tests pass, 10/10 integration tests pass, make lua-js-perf-check passes. Key changes: enabled bytecode VM for property access ops, added bytecode compilation to eval_script_body for top-level scripts, added CallMethod opcode, builtin marker resolution in VM, string method markers, JSON/Error/Object/Array builtins in call_builtin_method, ArithParser closure fix, string+int concat optimization, SADD single-member fast path.

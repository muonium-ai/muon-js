---
id: T-000074
title: "Browser demo: move leaderboard to right column, reduce graph height"
status: done
priority: p2
type: code
effort: s
labels: [mini-redis, wasm, webgpu, ui]
tags: [muonjs]
owner: null
created: 2026-02-22
updated: 2026-02-22
depends_on: [T-000073]
branch: null
---

## Goal
Improve the web demo layout so the leaderboard sits in a vertical column on the right side of the dashboard graph, and the graph canvas is made smaller to accommodate.

## Acceptance Criteria
- [x] Move leaderboard from below the graph to a right-side column next to the graph.
- [x] Leaderboard displays each player on a single row (vertical list, no grid wrapping).
- [x] Reduce the WebGPU canvas height/width to make room for the leaderboard column.
- [x] Layout remains usable on narrow viewports (stack vertically on mobile).

## Files
- `web/demo/index.html` — restructure dashboard + leaderboard into a flex/grid row.
- `web/demo/src/style.css` — update `.dashboard-wrap`, `.leaderboard-wrap`, `#gpuCanvas` styles.

## Notes
- Currently `#leaderboardList` uses `grid-template-columns: repeat(auto-fit, minmax(200px, 1fr))` which wraps players across columns. Change to a single-column vertical `<ol>`.
- Canvas is currently `1200x520`; reduce to something like `900x400` or smaller to fit side-by-side.

---
id: T-000004
title: Add fast paths for string coercion and replace helpers
status: needs_review
priority: p0
type: refactor
effort: m
labels:
- performance
tags:
- strings
owner: agent-1
created: '2026-02-20'
updated: '2026-02-20'
depends_on:
- T-000002
branch: bug/t-000004-add-fast-paths-for-string-coercion-and-r
---

## Goal
Reduce allocations in value-to-string conversions and string replace flows while preserving mquickjs behavior.

## Acceptance Criteria
- [ ] Reduce temporary `String` allocations in `value_to_string`, `regexp_parts`, and replace helper flows.
- [ ] Preserve observable behavior for `String.prototype.replace/replaceAll` substitution semantics.
- [ ] Add focused benchmarks for replace-heavy workloads and report before/after allocation/time deltas.
- [ ] Maintain compatibility for existing string/regex tests.

## Notes
- Candidate areas: `src/api.rs` (`value_to_string`, `regexp_parts`, `string_replace_nonregex`, `string_replace_regex`).
- Test plan:
	- Run string and regex sample suites from `tests/` and `samples/pass/`.
	- Compare benchmark results from ticket `T-000002` harness before and after changes.
	- Verify no change in outputs against `vendor/mquickjs` for covered replace cases.

## Progress Log
- 2026-02-20: Implemented string-path optimizations in src/api.rs: added non-regex literal replace fast path (replace/replacen when no '$' substitutions), reduced temporary allocations in substitution parsing, deferred regex capture vector allocation to non-function replacement path, and reused value_to_string/coerce flow in regexp_parts. Added regex replace workload to src/bin/bench_runtime.rs and updated performance.md docs. Validation: cargo test --lib passed (64/64); make js-runtime-bench passed with output tmp/comparison/js_runtime_benchmark_20260220_152038.json. string_replace_all median improved from ~528401 ops/s (20260220_151746) to ~575683 ops/s (20260220_152038).

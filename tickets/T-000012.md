---
id: T-000012
title: Add stable Redis-vs-mini performance gate
status: ready
priority: p0
type: tests
effort: m
labels: [performance]
tags: [benchmark, mini-redis, redis-parity]
owner: null
created: 2026-02-20
updated: 2026-02-20
depends_on: []
branch: null
---

## Goal
Create a deterministic, repeatable performance gate that compares Redis Lua and mini-redis JS scripting throughput across 3 runs using median and mean summaries.

## Acceptance Criteria
- [ ] Add a checked-in benchmark runner (script and/or Make target) that executes Redis Lua and mini-redis JS suites in 3 rounds with port isolation and startup readiness checks.
- [ ] Emit machine-readable outputs (JSON/CSV/TXT) including per-case RPS, per-round JS/Lua ratio, and mean/median summaries.
- [ ] Fail the gate (non-zero) when configured critical cases (`hash_sum`, `set_members`, `bulk_incr`) regress beyond a configurable threshold relative to baseline.
- [ ] Document baseline refresh and regression investigation workflow in `performance.md`.

## Notes
- Reference implementation areas: `scripts/bench_scripting.py`, `Makefile`, `tmp/compare_lua_js.py`.
- Test plan:
	- Run the 3-round comparison end-to-end twice and verify stable output schema.
	- Validate pass/fail behavior with controlled threshold changes.
	- Confirm `mt validate` and standard test flow still pass.

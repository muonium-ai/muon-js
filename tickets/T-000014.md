---
id: T-000014
title: Replace String command normalization with byte dispatch
status: needs_review
priority: p0
type: code
effort: m
labels: [performance]
tags: [mini-redis, dispatch]
owner: agent-1
created: 2026-02-20
updated: 2026-02-20
depends_on: [T-000012]
branch: bug/t-000014-replace-string-command-normalization-wit
---

## Goal
Reduce command dispatch overhead by replacing `to_upper_ascii`/`String` normalization with byte-level case-insensitive command matching.

## Acceptance Criteria
- [ ] Remove hot-path `to_upper_ascii` allocations for command names in client command dispatch (`handle_client`/`handle_command`) and script subcommands.
- [ ] Implement efficient case-insensitive byte matching that preserves existing command acceptance behavior.
- [ ] Keep unknown-command and arity error messages unchanged.
- [ ] Demonstrate reduced overhead on command-heavy benchmark cases (`PING`/`SET`/`GET` style and scripting `redis_call`).

## Notes
- Hotspot references: `to_upper_ascii` usage in `src/mini_redis/server.rs` and repeated `cmd.as_ref()` branching.
- Test plan:
	- Run unit/integration tests for command semantics.
	- Run benchmark suite before/after and capture per-case deltas.

## Progress Log
- 2026-02-20: Implemented byte-based top-level command dispatch in src/mini_redis/server.rs via parse_command(input: &[u8]) -> Option<&'static str>, removing String normalization/allocation from handle_client and redis_call command token paths. Unknown-command behavior preserved with ERR unknown command replies. Validation: cargo test --lib passed; make mini-redis-js-scripting-bench improved key cases (redis_call 24531.35 rps, incrby 24614.22, hash_sum 791.37, set_members 1865.70, bulk_incr 2079.91); make lua-js-perf-check passed with improved aggregate ratios.
